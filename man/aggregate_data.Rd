% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{aggregate_data}
\alias{aggregate_data}
\title{Aggregate line list datasets}
\usage{
aggregate_data(
  person_data = NULL,
  vaccination_data,
  outcome_data,
  cycle_start_date,
  cycle_end_date,
  site_code,
  design_selection = NULL,
  options_file_location = getwd(),
  options = NULL,
  suppression_limit = NULL,
  split_size = 5e+05,
  stop_after_n_splits = NULL,
  restore_input_data = TRUE,
  working_directory = getwd(),
  input_data_in_chunks = FALSE,
  final_chunk = FALSE,
  skip_user_prompts = FALSE,
  output_format = "parquet",
  patient_data = NULL,
  participation_level = NULL
)
}
\arguments{
\item{person_data}{Person data input data.table}

\item{vaccination_data}{Vaccination data input data.table. Set to NULL to perform historical data aggregation}

\item{outcome_data}{AESI outcome data input data.table}

\item{cycle_start_date}{Start date of cycle, as a date object, an 8 digit number of the form yyyymmdd, or a string of the form yyyy-mm-dd}

\item{cycle_end_date}{End date of cycle, as a date object, an 8 digit number of the form yyyymmdd, or a string of the form yyyy-mm-dd}

\item{site_code}{Site code (e.g. AUS_MCRI), as a string}

\item{design_selection}{Character vector of analytical study designs to run. Valid values are:
\code{"self_post"}, \code{"self_pre"}, \code{"historical"}, \code{"concurrent_vac"}, \code{"concurrent_unvac"}.
Default is NULL, which runs all five designs.
Descriptive outcomes and vaccination datasets are always produced for non-historical aggregations
and cannot be excluded via this parameter.}

\item{options_file_location}{Location where options.txt file is stored. Supply either \code{options_file_location} or \code{options}, not both. Defaults to \code{getwd()}}

\item{options}{Options object, already converted from txt file. Supply either options_file_location or options, not both}

\item{suppression_limit}{If required, low numbers can be suppressed. Numbers less than the limit will be suppressed, except 0}

\item{split_size}{Input data will be automatically split into pieces of this size (default 500,000). If memory is an issue, lower the split_size.
Aggregation is done on the split data separately, and then combined once all splits are complete.
Aggregation can be stopped and restarted from the last completed split (see \code{resume_cycle_aggregation()} function)}

\item{stop_after_n_splits}{Use this to stop aggregation after a given number of splits. Aggregation can then be resumed at a later date (see \code{resume_cycle_aggregation()} function)}

\item{restore_input_data}{If TRUE (default) the input data will be restored from the data folder to the global environment once the aggregation is complete,
or stop_after_n_splits is reached. If FALSE, the input data will not be restored.
WARNING: the data folder is removed once aggregation is complete (not if stopped using \code{stop_after_n_splits})}

\item{working_directory}{Location of working directory. This is where the folder for this cycle will be created. Defaults to \code{getwd()}.}

\item{input_data_in_chunks}{Option to pre-split input data. Only use this if your data is too large to load into R in one go (otherwise just use split_size)}

\item{final_chunk}{If you have pre-split your input data, use this to indicate when you are running the last chunk of your input data. This will trigger the function
to combine all the temporary results at the end}

\item{skip_user_prompts}{Boolean allowing user prompts to be skipped, mainly for the purpose of testing for which prompts are not possible. Recommended to leave as FALSE.}

\item{output_format}{Output file format: \code{"parquet"} (default, recommended for efficiency
and cross-language compatibility) or \code{"csv"} (for legacy workflows).}

\item{patient_data}{Deprecated. Use \code{person_data} instead.}

\item{participation_level}{Deprecated. Use \code{design_selection} instead.
Previously controlled which designs were available: 1 = minimal, 2 = partial, 3 = full.}
}
\description{
This function will take the input datasets and create a folder containing the aggregated tables
as separate parquet files, depending on your design selection. A separate folder is created for each cycle period.

Aggregation can be stopped and resumed (see \code{resume_cycle_aggregation()} function).

To trigger historical data aggregation (i.e. before vaccinations began), set \code{vaccination_data = NULL}.

Requires an 'options.txt' file to run - in a multi-site study this is provided by the coordinating centre; in a single-site study you create it yourself (see \code{vignette("options", "rapidcycler")}).

Validation of the input datasets is automatically run as part of this function, and will throw an error if it finds any issues that require fixing.
If you would like to run validation separately, use the \code{validate_input_data()} function.
}
\examples{
\dontrun{
# Generate synthetic data and aggregate
data <- generate_synthetic_data(pop_size = 1000, save_data = FALSE)

aggregate_data(
  person_data = data$person_data,
  vaccination_data = data$vaccination_data,
  outcome_data = data$outcome_data,
  cycle_start_date = "2021-01-01",
  cycle_end_date = "2023-01-01",
  site_code = "TEST_SITE",
  options_file_location = "path/to/options",
  skip_user_prompts = TRUE
)
}

}
