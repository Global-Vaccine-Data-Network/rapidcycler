# Check format of date

Checks that the inputted date is of a recognisable format, and if so,
converts it to a date object, otherwise throws an error

## Usage

``` r
check_date_format(date)
```

## Arguments

- date:

  Character, number, or date object, representing a date.

## Value

Date object
