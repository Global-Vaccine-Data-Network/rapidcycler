# Check options object

Checks that the options object has the correct format and contains all
the required information. If not, throws an error.

## Usage

``` r
check_options_object(options)
```

## Arguments

- options:

  Options object in list format
