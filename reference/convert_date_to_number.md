# Convert date to number

Converts a date object into an 8 digit integer representing a date in
the format yyyymmdd.

## Usage

``` r
convert_date_to_number(date)
```

## Arguments

- date:

  Date object

## Value

8 digit integer representing a date in the format yyyymmdd
